# ABLE Workflow Module Copier module `{{ module_type }}/{{ module_name }}`

{{ module_description }}

??? note "Audience: Project Developers"

    This page is for project developers updating files that were originally generated by the `able-workflow-module-copier` template.
    If you just ran copier and are implementing the new scaffold for the first time, start with the post-copy checklist instead.

## Updating module

To update this module, run the following command in the root of your project:

```bash
conda activate {{ package_name }}-py312-dev
copier update --trust --answers-file ".copier-answers/module-{{ module_type }}-{{ module_name }}.yml"
```
